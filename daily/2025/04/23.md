---
id: "23"
aliases: []
tags: []
---

- [x] relook at tickets and add more details
- [x] scaffold base for index pages so others can do
- [x] file new ticket for index page to have thumbnail
- [x] add thumbnail for article page
- [x] write dogfoodign doc
- [x] add story for index page header iwth row layout
- [x] add doc for banner on egazette and test

# index page

- we need to pass `permalink` to the component so that we can render the preview properly
  - either that or we get data on the fly and pass
- need to set a special property `childpages` on `IndexPages` and declare it on the schema
- write down distinction between `layout` and `type`
- shift `pos` from `Resource` to `Blob`
- need to update the schemas fr other pages to accept `undefined` for `childpages`

## dogfooding for index pages

- [ ] need to create the custom index pages
  - [ ] old `index` without custom content
  - [ ] `index` with custom content
  - [ ] `content` with custom content
- [ ] user persona
  - [ ] existing user who has custom `layout: content` pages -> test that it still works and maintains backcompat (the backcompat is the main thing that we are checking for)
  - [ ] existing user who has `layout: index` page
  - [ ] with custom content -> should still be able to see custom content, should still see default child blocks, should be able to change title and summary, should be able to upload thumbnail to child pages
  - [ ] without custom content -> should still be able to change title/summary, should be able to upload thumbnail
